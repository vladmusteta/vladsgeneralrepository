apiVersion: v1
kind: PersistentVolume
metadata:
  name: grafana-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /home/k8svolumes/grafana/mounted_storage ## Change to the desired path
    type: DirectoryOrCreate
  persistentVolumeReclaimPolicy: Retain
