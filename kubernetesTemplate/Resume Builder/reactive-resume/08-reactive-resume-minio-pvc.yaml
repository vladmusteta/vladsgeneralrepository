apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: reactive-resume-minio-pvc
  namespace: reactive-resume
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: local-storage
  volumeName: reactive-resume-minio-pv