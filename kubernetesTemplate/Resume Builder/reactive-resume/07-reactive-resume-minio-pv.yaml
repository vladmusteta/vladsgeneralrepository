apiVersion: v1
kind: PersistentVolume
metadata:
  name: reactive-resume-minio-pv
  namespace: reactive-resume
spec:
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  hostPath:
    path: /home/k8svolumes/reactive-resume/mounted_volume/minio_data
    type: DirectoryOrCreate