apiVersion: v1
kind: Service
metadata:
  name: glance-service
  namespace: dashboard
spec:
  type: NodePort
  selector:
    app: glance
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30880
    protocol: TCP
    name: http